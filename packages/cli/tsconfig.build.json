{
  "extends": "../../tsconfig/node.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./bin",
    "lib": ["dom"],
    "noUnusedLocals": false,
    // !!!: Don't output declaration or source maps
    // !!!: This is a CLI, not a library, no need.
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false
  },
  "exclude": ["node_modules", "bin", "dist"]
}
