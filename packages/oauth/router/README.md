# @iracing-data/oauth-router

A better-call router for the iRacing OAuth services.

Adds a `/signin`, `signout`, and configurable callback API for the iRacing OAuth flow.

Example can be found in [iracing-proxy-api-example](../../../examples/iracing-proxy-api-example).

## Installation

## Usage
