{
  "name": "iracing-oauth-example",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "dev": "tsx watch --tsconfig tsconfig.build.json --env-file=.env src/index.ts"
  },
  "dependencies": {
    "@iracing-data/oauth-client": "workspace:*",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "tsx": "^4.19.0"
  }
}